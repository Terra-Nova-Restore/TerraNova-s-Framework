# MMD-006 Apply Gate

Status: applied

## Exact Authorization Required

`GO Notion MMD-006 anwenden`

This exact command was received and used as the mutation authorization.

## Applied Live Changes

- CAP-MOD-DRAFT-516 - Inspiration
- CAP-MOD-DRAFT-520 - SessionStart
- CAP-MOD-DRAFT-521 - Preflight
- CAP-MOD-DRAFT-540 - Observable Momentum
- CAP-MOD-DRAFT-544 - Synchronization Node

Additional applied live changes:

- create registry view `CAP Module Drafts`
- create registry view `CAP Module Sync Needed`
- append a short MMD-006 checkpoint to the CAP page

## Stop Rules Preserved

The apply was bounded by these stop rules:

- registry schema differs from `notion-registry-field-map.md`
- a row with the same exact `Name` already exists
- Notion connector cannot verify the target registry
- any target would require raw private IDs in the output
- the action would assign canonical `TRG-*` IDs
- the action would expand `517`, `777` or `988-992`

## Verification After Apply

- registry rows found by `CAP-MOD-DRAFT` search
- both views found in registry fetch
- `mmd-006.registry-updates.csv` written with applied targets
- `python scripts/cap_control_checks.py --live-zenodo` rerun after local trace update
