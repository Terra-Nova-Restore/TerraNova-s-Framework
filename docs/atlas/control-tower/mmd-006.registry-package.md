# MMD-006 - CAP Module Registry Package

Status: applied

Date: 2026-05-17

## Decision

MMD-006 prepared and then applied a Notion-safe registry package for the five MMD-005 CAP module drafts.

The live mutation was performed only after the exact authorization phrase `GO Notion MMD-006 anwenden`.

## Registry Rows

| Name | Trigger Ref | Feedback Target | Sync Status |
| --- | ---: | --- | --- |
| CAP-MOD-DRAFT-516 - Inspiration | `516` | Trigger Map | Needs sync |
| CAP-MOD-DRAFT-520 - SessionStart | `520` | Logbook | Needs sync |
| CAP-MOD-DRAFT-521 - Preflight | `521` | Logbook | Needs sync |
| CAP-MOD-DRAFT-540 - Observable Momentum | `540` | Prism / Zenodo | Needs sync |
| CAP-MOD-DRAFT-544 - Synchronization Node | `544` | Registry | Needs sync |

## Package Outputs

| File | Purpose |
| --- | --- |
| `mmd-006.registry-package.csv` | Five import/update-ready registry rows. |
| `mmd-006.registry-field-map.csv` | Schema field mapping and value rules. |
| `mmd-006.view-package.csv` | Two proposed registry views. |
| `mmd-006.notion-mutation-package.csv` | Exact external mutations requiring explicit GO. |
| `mmd-006.apply-gate.md` | Human-readable application gate and stop rules. |
| `mmd-006.review-summary.json` | Machine-readable package summary. |

## Application Boundary

Apply only after exact command: `GO Notion MMD-006 anwenden`

MMD-006 blocks:

- deletion
- schema change
- raw private Notion IDs
- canonical `TRG-*` assignment
- expansion of `517`, `777` or `988-992`
- publication or Zenodo action

## Next

Best next action: keep MMD-006 rows in `Needs sync` until the next source-review pass can decide whether any draft module is mature enough to move from Candidate toward a stronger canon status.

## Applied Verification

Applied on 2026-05-17:

- five registry rows created
- `CAP Module Drafts` view created
- `CAP Module Sync Needed` view created
- CAP page checkpoint appended

Applied trace:

```text
docs/atlas/control-tower/mmd-006.registry-updates.csv
docs/atlas/control-tower/causal-log.mmd-006-mutation-2026-05-17.json
```
