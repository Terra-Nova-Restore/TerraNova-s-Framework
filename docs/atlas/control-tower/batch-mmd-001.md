# MMD-001 - Mermaid Universe Readpass

Date: 2026-05-17

Activation: `GO MMD-001`

Mode: read-only Notion research plus repo-local synthesis

External mutation: none

Notion AI credits used: 0

## Purpose

MMD-001 pauses direct trigger reconstruction and moves the understanding work
through the Mermaid universe first.

The decision is operational:

- do not force the 675-trigger field into a linear register now
- use Mermaid as the visual control surface for trigger comprehension
- keep SCL and deep decoder work outside this pass
- preserve VORTEX as the higher-order coherence layer

## Boundary

MMD-001 does not:

- edit Notion
- create Notion Custom Agents
- run Notion AI automations
- reconstruct Trigger 1-400 line by line
- expose raw private trigger or Schattenarchiv material
- claim that blank diagram pages contain missing code

## Source Pass

Read-only Notion workspace search was forced to `workspace_search`.
The pass did not use Notion AI semantic search or agent execution.

Primary Notion sources:

| Source | Role | Finding |
| --- | --- | --- |
| Mermaid Atlas - Start Here | Navigation | Defines IST, SOLL, INTEGRATION and ORCHESTRATION as four chef diagrams. |
| Mermaid Diagrams - Code Library | Registry | Provides role, status, type, zoom and source properties. |
| Mermaid Code Library - Complete Collection | Depot/wiki | Container for rendered diagrams and code depot. |
| Mermaid als lebendiger Trigger-Organismus | Concept | Defines node as trigger entity, edge as activation condition, graph as living system. |
| Mermaid-Diagramme als lebende Systeme | Technical essay | Expands executable architecture, guard conditions, state-aware rendering and exports. |
| VortexCanvas - Systemuebersicht | VORTEX bridge | Defines VortexCanvas as visual decision and resonance field, not a timeline. |
| VortexCanvas - Entscheidungsraeume | VORTEX bridge | Frames VortexCanvas as the minimal operating unit for decision spaces. |

Primary GitHub sources:

| Source | Role | Finding |
| --- | --- | --- |
| `docs/atlas/diagrams.md` | Local diagram registry | Confirms 9 active diagrams, 8 legacy rows and no detected orphans. |
| `docs/ai/cic_atlas_usage.md` | Routing note | Treats Mermaid registry as diagram status truth. |
| `docs/ai/full_sync_terra_nova_mcp_sequence.md` | Sequence example | Shows existing Mermaid as executable sync architecture. |
| `raw/exports/prism/source-pack/2026-05-02/` | Provenance | Contains exported Mermaid pages and registry CSVs. |

## Result

The Mermaid universe is a better first control surface than the raw trigger
list because it already separates:

- current state: IST
- target state: SOLL
- data and sync flow: INTEGRATION
- routing and responsibility flow: ORCHESTRATION
- detail lenses: ZOOM
- code and appendix layer: DEPOT / APPENDIX

This gives CAP a usable visual grammar before trigger hashes or deep trigger
stories are attempted.

## Outputs

| File | Purpose |
| --- | --- |
| `mmd-001.mermaid-universe-readpass.md` | Main synthesis and CAP interpretation. |
| `mmd-001.diagram-inventory.csv` | Active diagram and concept source inventory. |
| `mmd-001.relation-map.csv` | Mermaid universe relation map for CAP routing. |
| `causal-log.mmd-001-readpass-2026-05-17.json` | Causal trace for the MMD-001 decision. |

## Next

Best next move after this pass:

```text
MMD-002 - Mermaid Graph Extraction
```

MMD-002 should parse the active Mermaid registry and exported source pack into a
normalized node/edge table. It should still avoid SCL depth work and should not
touch Notion without a separate explicit `GO Notion`.
