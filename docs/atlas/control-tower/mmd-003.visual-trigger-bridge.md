# MMD-003 - Visual Trigger Bridge

Status: completed

Date: 2026-05-17

## Decision

MMD-003 turns the extracted Mermaid graphs into trigger module candidates.

This is still not the canonical trigger registry. It is a bridge layer:

```text
Mermaid node / edge -> visual evidence -> module candidate -> later canon gate
```

## Summary

| Output | Rows |
| --- | ---: |
| `mmd-003.visual-trigger-candidates.csv` | 53 |
| `mmd-003.guard-bridge.csv` | 140 |
| `mmd-003.sensitivity-review.csv` | 20 |

Visible trigger references:

```text
174-210, 516, 517, 520, 521, 540, 544, 600, 777, 988, 989, 990, 991, 992
```

Candidate classes:

| Class | Count |
| --- | ---: |
| numeric_anchor | 16 |
| numeric_range | 1 |
| trigger_surface | 18 |
| sync_surface | 13 |
| audit_surface | 4 |
| codex_surface | 1 |

Sensitivity split:

| Sensitivity | Count |
| --- | ---: |
| Internal | 45 |
| Sensitive | 7 |
| Restricted | 1 |

## High-Value Internal Candidates

The strongest immediate bridge is still the Codex/Trigger zoom graph:

| Candidate | Evidence | Status |
| --- | --- | --- |
| `516` | `Trigger 516: Inspiration` | visible number only |
| `520` | `Trigger 520: SessionStart` | visible number only |
| `540` | `Trigger 540: Beobachtbares Momentum` | visible number only |
| `544` | `Trigger 544: Synchronisationsknoten` | visible number only |
| `174-210` | `Trigger 174-210: 37 neue` | visible range only |
| `600` | `Triggerliste_600` | visible list reference |

The SESSION_ROOT graph adds execution-shaped relations:

| Relation | Guard |
| --- | --- |
| `T520 -> T521` | `session_opened` |
| `T521 -> ROOT` | `preflight_ok` |
| `T13 -> T777` | `audit_kernel` |
| `T14 -> T517` | `autoflow_on` |
| `T15 -> T544` | `delta_confirmed` |

## Gated Material

The following rows are held for SENS-001 review before expansion:

- `777` / Schattenarchiv
- `988-992` TokenAccess / integrity suite
- token/cursor/auth storage relations in the sync sequence

These are useful signals, but they are not routine operating commands.

## Control Rule

Every row remains a candidate until it passes a canon gate.

Allowed now:

- use visual candidates for navigation
- use guards to understand route and activation shape
- prepare CAP module lanes

Not allowed now:

- claiming a complete trigger canon
- publishing restricted trigger semantics
- assigning canonical `TRG-*` IDs from visual evidence alone
- treating extracted edge labels as execution permission

## Repeat Command

```powershell
python scripts/build_visual_trigger_bridge.py
```

## Next

MMD-004 should review and split candidates:

- Safe internal module candidates
- Sync/control candidates
- Canon-source-needed candidates
- SENS-held candidates

Only after that should CAP create stable trigger-module records.
